#include <iostream>
using namespace std;

int day3_remind() {
	// 비트 연산자 복습
	// & - and, | - or
	// and - 0을 넣으면 다 0이 됨. 검출하고 싶은 자리가 0 인지 1인지 판별에 사용
	// or - 하나라도 1이면 1. 특정 자리에 값을, 1을 추가해주는 역할
	// ^ - xor, 특정 자리의 비트를 반전시키는 역할. 원하는 자리에 1을 넣으면 비트반전
	// ~ - not, 모든 비트 반전
	// <<, >> - 왼쪽, 오른쪽 shift 한칸 옮기고 끝트머리는 삭제됨

	return 0;
}